# A Shape node is a visual objects that includes both an 'appearance' and a 'geometry'.
# A Shape can also be used in a 'boundingObject', in this case, only its 'geometry' field is taken into account.

Shape {
  vrmlField SFNode appearance  NULL
  vrmlField SFNode geometry    NULL
  field     SFBool castShadows TRUE
  field     SFBool isPickable  TRUE
}
