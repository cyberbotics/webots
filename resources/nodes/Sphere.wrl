# The Sphere node specifies a sphere primitive centered at (0,0,0) in the local coordinate system.

Sphere {
  vrmlField SFFloat radius      1
  field     SFInt32 subdivision 1
  field     SFBool  ico         TRUE # defines if the sphere is an icosphere (TRUE) or a UV sphere (FALSE)
}
