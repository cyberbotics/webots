# Placeholder CMakeLists.txt for resources/projects/libraries
# TODO: Convert the logic from the corresponding Makefile section to CMake.

cmake_minimum_required(VERSION 3.10)
project(webots_resources_projects_libraries)

message(STATUS "Configuring resources/projects/libraries")

# Add build targets, sources, and installation rules for libraries here.