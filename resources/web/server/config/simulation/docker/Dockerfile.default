ARG WEBOTS_DEFAULT_IMAGE
FROM $WEBOTS_DEFAULT_IMAGE
ARG PROJECT_PATH
ARG MAKE
RUN mkdir -p $PROJECT_PATH
COPY . $PROJECT_PATH
RUN if [ "$MAKE" = "1" ]; then \
    cd $PROJECT_PATH && \
    make ; \
    fi
