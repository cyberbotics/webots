ARG WEBOTS_DEFAULT_IMAGE
FROM $WEBOTS_DEFAULT_IMAGE
ARG MAKE, PROJECT_PATH
RUN mkdir -p $PROJECT_PATH
COPY . $PROJECT_PATH
RUN if [ "$MAKE" = "1" ]; then \
    cd $PROJECT_PATH && \
    make ; \
    fi