#VRML_SIM R2022a utf8

PROTO ParserTestSupervisor [
]
{
  Robot {
    children [
      Receiver {
        name "ts_receiver"
        channel 20
      }
    ]
    name "parser test supervisor"
    controller "test_suite_supervisor"
    supervisor TRUE
    customData "parser"
  }
}
