#VRML_SIM R2020a utf8

PROTO templateContextPrint
[
]
{
  %{
    if context.webots_home ~= nil and context.webots_version ~= nil and context.webots_version.revision ~= nil and context.project_path ~= nil and context.proto then
      print(context.world)
    end
  }%
  Solid {
  }
}
