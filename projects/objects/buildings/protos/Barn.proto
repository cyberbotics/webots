#VRML_SIM R2022b utf8
# license: Copyright Cyberbotics Ltd. Licensed for use only with Webots.
# license url: https://cyberbotics.com/webots_assets_license
# A Barn.
# Based on the work of linus1178 (https://sketchfab.com/linus1178)
# template language: javascript

PROTO Barn [
  field SFVec3f    translation  0 0 0
  field SFRotation rotation     0 0 1 0
  field SFFloat    scale        1      # Defines the global scaling factor.
  field SFString   name         "barn"
]
{
  %<
    let scale = fields.scale.value;
    if (scale <= 0) {
      scale = fields.scale.defaultValue;
      console.error('\'scale\' must be strictly positive. Value reset to ' + scale + '.');
    }
  >%
  Solid {
    translation IS translation
    rotation IS rotation
    name IS name
    model "cow"
    scale %<= scale >% %<= scale >% %<= scale >%
    children [
      Shape {
        appearance PBRAppearance {
          metalness 0
          roughness 0.7
          baseColorMap ImageTexture {
            url "webots://projects/objects/buildings/protos/barn/textures/BaseColorBarn.png"
          }
        }
        geometry Mesh {
          url "webots://projects/objects/buildings/protos/barn/meshes/barn.obj"
        }
      }
    ]
    boundingObject Group {
      children [
        Transform {
          translation 0 0 1.5
          children [
            Box {
              size 3.3 2.8 3
            }
          ]
        }
      ]
    }
  }
}
