ifeq ($(OS),Windows_NT)
TARGET = client.exe
LIBS = -lws2_32
CXX=g++
else
TARGET = client
LIBS =
endif

.PHONY = build release debug profile

build release debug profile: $(TARGET)

$(TARGET): client.cpp
	$(CXX) $< -o $@ $(LIBS)

clean:
	rm -f client.o client client.exe
