#VRML_SIM R2020a utf8
# license: Apache License 2.0
# license url: http://www.apache.org/licenses/LICENSE-2.0
# A grass material useful with the `Floor` PROTO.

PROTO Grass [
  field SFColor colorOverride    1 1 1  # Defines the default color multiplied with the texture color.
  field SFString{"maintained", "prickly", "mossy"} type "maintained"  # Defines the grass type.
  field SFNode  textureTransform NULL   # Defines an optional 2d texture transform.
  field SFFloat IBLStrength      1      # Defines the strength of ambient lighting from the Background node.
]
{
  %{
    local typeID = 1
    if fields.type.value == "prickly" then
      typeID = 2
    elseif fields.type.value == "mossy" then
      typeID = 3
    end
  }%
  PBRAppearance {
    baseColor IS colorOverride
    baseColorMap ImageTexture {
      url [
        %{= "\"textures/grass/grass_" .. typeID .. "_base_color.png\"" }%
      ]
    }
    roughness 1
    %{ if typeID == 2 then }%
      roughnessMap ImageTexture {
        url [
          %{= "\"textures/grass/grass_" .. typeID .. "_roughness.png\"" }%
        ]
      }
    %{ end }%
    metalness 0
    IBLStrength IS IBLStrength
    normalMap ImageTexture {
      url [
        %{= "\"textures/grass/grass_" .. typeID .. "_normal.png\"" }%
      ]
    }
    occlusionMap ImageTexture {
      url [
        %{= "\"textures/grass/grass_" .. typeID .. "_occlusion.png\"" }%
      ]
    }
    textureTransform IS textureTransform
  }
}
